{
    "name": "wxapp-template",
    "version": "0.0.5",
    "scripts": {
        "install": "git init && git rm --cached --ignore-unmatch we && rm -rf .git/modules/we && rm -rf we && git submodule add https://github.com/2046/we.git && rm -rf ./node_modules"
    }
}